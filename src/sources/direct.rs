// Direct URL crawling is handled by the shared `publish_url_batch` in main.rs.
// This module is reserved for future direct-crawling enhancements
// (sitemap parsing, robots.txt, recursive link following).
